// Use a "for" loop to iterate in reverse

const myReverseStringArray = ['fried-rice', 'chicken', 'pepsi']
for (let i = myReverseStringArray.length - 1; i >= 0; i--) {
  const food = myReverseStringArray[i]
  console.log(food)
}
